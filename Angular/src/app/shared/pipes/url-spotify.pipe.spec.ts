import { UrlSpotifyPipe } from './url-spotify.pipe';

describe('UrlSpotifyPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlSpotifyPipe();
    expect(pipe).toBeTruthy();
  });
});
