import { ShortenTimePipe } from './shorten-time.pipe';

describe('ShortenTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenTimePipe();
    expect(pipe).toBeTruthy();
  });
});
