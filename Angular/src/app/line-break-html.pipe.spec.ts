import { LineBreakHtmlPipe } from './line-break-html.pipe';

describe('LineBreakHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
